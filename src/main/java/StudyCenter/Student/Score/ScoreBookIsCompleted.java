package StudyCenter.Student.Score;

/**
 * determines if course completed
 */
public class ScoreBookIsCompleted {
    Boolean completed;

    ScoreBookIsCompleted(ScoreBook scoreBook) {
        //check if completed
    }

    public Boolean getCompleted() {
        return completed;
    }
}
